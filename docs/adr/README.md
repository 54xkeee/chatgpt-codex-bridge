# ADR

Add an ADR when you make a significant decision (library, architecture, irreversible change).
Use a simple format: Context / Decision / Consequences.

Latest decision: ADR-0018 keeps Windows Tunnel exit recovery inside the existing
owned command loop and preserves process-tree lifecycle control.
