# v0.6.1 Security Hardening Verification

Date: 2026-08-23
Result: `PASS`; source, anonymous export and public release verified

## Closed findings

1. Context-bound installation/workspace/policy capabilities.
2. Complete verified worker-process-group revocation on lifecycle operations.
3. Bridge-owned capability/job state purge during uninstall.
4. UTF-8-safe capability rejection and common prompt bounds.
5. Bounded synchronous concurrency and deadline.
6. Codex result separation from user-role Apps recovery messages.
7. Tunnel checksum/archive/payload validation before candidate execution.
8. Repository-external private denylist with synthetic public fixtures.
9. Removal of device/account/network/latency telemetry from publication truth.

## Fresh evidence

- Guard contract suite: `50/50 PASS`.
- Guard source/package byte identity: `PASS`.
- Python compilation and all production Zsh syntax checks: `PASS`.
- Bridge preflight, repository boundary, Codex resolver and Tunnel verifier
  suites: `PASS`.
- Portable plugin and isolated macOS install/doctor/uninstall suites: `PASS`.
- Current tracked-tree sanitization and synthetic negative fixtures: `PASS`.
- Working-tree Gitleaks directory scan: zero findings.
- Index-based anonymous export: Gitleaks zero findings; sanitization, plugin
  package and clean-HOME installer suites all `PASS`.
- `git fsck --full --strict`: completed without integrity errors; unreachable
  dangling objects were informational private-repository residue.

## Publication boundary

The private repository remains authoritative and retains its audit history. The
public repository must be rooted at the exact verified v0.6.1 tree so
superseded privacy material is not reachable from a public branch or tag.
Removing public refs cannot revoke third-party caches; no usable credentials
were found, so no credential rotation is required by this tranche.

Published release evidence:

- public `main` and `chatgpt-codex-bridge-v0.6.1` were rooted at the verified
  sanitized release commit;
- the superseded v0.6.0 tag was removed from public refs;
- an anonymous depth-one clone resolved to a parentless release commit;
- public-history Gitleaks scanned one commit with zero findings;
- the anonymous clone passed sanitization and plugin package tests.
